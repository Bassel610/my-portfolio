'use client';
import { Typography } from '@mui/material';

export default function Heading() {
  return (
    <Typography
      position="relative"
      top="-19px"
      variant="h4"
      component="h2"
      sx={{ color: 'white', mb: 4, display: 'inline-block' }}
    >
      Work Experience
    </Typography>
  );
}
