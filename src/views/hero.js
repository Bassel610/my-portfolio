import { Section } from '@/components/shared';
import Hero from '@/components/hero';

export default function HeroView() {
  return (
    <Section id="hero" py={false}>
      <Hero />
    </Section>
  );
}
